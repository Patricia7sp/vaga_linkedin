# The main pipeline for vagas_linkedin
resources:
  pipelines:
    vagas_linkedin_pipeline:
      name: vagas_linkedin_pipeline
      catalog: workspace
      schema: vagas_linkedin_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
